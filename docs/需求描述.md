# 需求描述

做一个windows pc上运行的批量压缩图片和视频的工具，支持压缩后的图片和视频文件按原格式的输出。

- 支持选择目录，批量压缩目录下的所有图片和视频文件。
- 支持自定义压缩比例，默认压缩比例为50%。
- 支持自定义输出目录，默认输出目录为原目录。
- 输出文件名添加"_compressed"后缀。

### 软件格式

- 可执行文件
- 无安装过程

### UI要求

- 美观，用户操作简单。
- 支持中文显示。
